# Виртуальные окружения
.venv/
venv/
env/

# Переменные окружения (секреты)
.env

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.ruff_cache/

# База данных не хранится в git — собирается из миграций + db/seed.sql
# (python scripts/init_db.py). Служебные файлы SQLite тоже игнорируем.
data/welding_shop.db
*.db-journal
*.db-shm
*.db-wal

# Локальные бэкапы БД (не коммитим — только страховка на машине)
data/backups/
backups/

# IDE / ОС
.idea/
.vscode/
.DS_Store
Thumbs.db

# Node (наследие прежней версии, если появится снова)
node_modules/
dist/
.claude/
